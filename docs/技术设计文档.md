# AITMK 客服系统技术设计文档（基于当前代码实现）

## 1. 文档目标

本文档描述当前实现的技术架构、模块职责、核心流程、数据结构、外部接口契约与运行机制，用于开发维护与后续迭代。

---

## 2. 技术栈与依赖

- Spring Boot 3.3.3
- Spring MVC（REST）
- Spring WebSocket + STOMP
- Spring WebFlux WebClient（对外 HTTP 调用）
- Jackson（JSON）
- Lombok

关键依赖：
- `spring-boot-starter-web`
- `spring-boot-starter-webflux`
- `spring-boot-starter-websocket`
- `spring-boot-starter-validation`

---

## 3. 模块划分

## 3.1 Controller 层

1. `WhatsAppWebhookController`
   - `GET /webhook`：Meta webhook 验证
   - `POST /webhook`：接收 WhatsApp 回调并异步处理

2. `AuthController`
   - `POST /api/auth/login`：登录、补分配、推送历史
   - `POST /api/auth/logout`：登出、更新离线状态

3. `ChatController`
   - `GET /api/chat/customers`
   - `GET /api/chat/customers/serving`（返回 serviceStatus/canReply）
   - `GET /api/chat/messages`
   - `POST /api/chat/reply`（含24小时校验）
   - `POST /api/chat/media/upload`（multipart 上传到 Meta，返回 mediaId）
   - `POST /api/chat/reply/media`（支持 mediaId 与 link 兼容）

4. `AgentController`
   - `POST /api/agent/ws/reconnected`：重连后重推失败消息
   - `POST /api/agent/activity/ping`：前端活跃心跳

---

## 3.2 Service 层

1. `WhatsAppWebhookServiceImpl`
   - 核心编排：入站消息、AI、分配、推送、落库、24h可回复标记、30天关闭重开

2. `InMemoryAgentDispatchService`
   - 在线坐席集合
   - 客户-坐席分配映射
   - 未分配客户队列
   - 轮询分配算法
   - 本地会话解绑（30天关闭场景）

3. `InMemoryChatHistoryService`
   - 会话历史存储
   - 查询客户列表/消息
   - 获取最后客户消息时间

4. `WebSocketAgentPushServiceImpl`
   - 消息推送（history/new_message）
   - 失败缓存
   - 重连重推

5. `CrmOpenApiServiceImpl`
   - CRM 查询/写入适配
   - 登录校验、登录状态更新、分配记录、聊天记录
   - 在线坐席/分配/聊天历史拉取
   - 服务中记录关闭（editRow）

6. `CrmCacheSyncServiceImpl`
   - 启动同步（`@PostConstruct`）
   - 手动同步入口（登录时调用）

---

## 3.3 Config 层

1. `CorsConfig`：全局跨域
2. `JacksonConfig`：时间类型序列化支持
3. `WebSocketConfig`：`/ws` 端点 + `/topic` broker
4. `CrmConfig`：CRM 连接参数

---

## 4. 核心流程时序（文字版）

## 4.1 客户消息到达

1. Webhook 接收 payload。
2. 解析消息，统一手机号格式，构造文本/媒体/位置消息内容。
3. 查询本地分配状态：
   - 若已有分配且坐席在线：
     - 写本地 history（customer）+ CRM（客户）
     - 直接 `pushNewMessage` 给当前坐席
     - 不再触发 AI
   - 若已有分配但坐席离线：
     - 保持分配关系不变
     - 由 AI 自动回复兜底
   - 若已有分配且客户最后消息超过 24h：
     - 将分配记录“是否可回复”更新为“否”
   - 若已有分配且客户最后消息超过 30 天：
     - 调 CRM `editRow` 将原服务中分配记录更新为“已关闭”
     - 清理本地分配关系，按新会话处理
4. 若当前未分配：
   - 先写本地 history（customer）+ CRM（客户）
   - 若无在线坐席：标记未分配队列
   - 走 AI 自动回复流程：发 WhatsApp、写本地 history（ai）、写 CRM（AI）
   - 若有在线坐席：调用 `assignIfAbsent`
     - 成功：写 CRM 分配记录（服务中）+ `pushHistory`
     - 失败：进入 pending 队列

## 4.2 坐席登录

1. 登录前执行 CRM 同步（恢复缓存）。
2. CRM 校验账号密码。
3. 成功后写登录记录（在线）并缓存 loginRecordRowId。
4. 加入在线集合。
5. 逐个领取未分配客户 -> 写分配记录 -> 推送完整历史。

## 4.3 坐席登出

1. 从在线集合移除。
2. 用 loginRecordRowId 调 CRM `editRow` 更新状态“离线”。
3. 不释放既有客户分配关系（仅影响新会话分配候选）。

## 4.4 人工回复

### 4.4.1 文本
1. 校验客户最后消息时间 <=24h。
2. 调 WhatsApp 发送文本。
3. 写本地历史（agent）+ CRM（人工）。

### 4.4.2 多媒体
1. 校验客户最后消息时间 <=24h。
2. 校验 `mediaId` / `mediaUrl` 至少提供一个。
3. 调 `sendMediaMessage`：优先 `id`，回退 `link`。
4. 写本地历史（记录 mediaId/filename/url/caption）+ CRM（人工）。

### 4.4.3 本地文件上传
1. `POST /api/chat/media/upload` 接收 multipart。
2. `SendMessageServiceImpl.uploadMedia` 组装 multipart 并调用 Meta `/{from}/media`。
3. 解析返回 JSON 的 `id` 作为 `mediaId` 返回前端。

## 4.5 WebSocket 重连与活跃心跳

1. 客户端重连后调用 `ws/reconnected`。
2. 服务端取失败缓冲并重发。
3. 客户端建议每 20~30 秒调用 `POST /api/agent/activity/ping`。
4. 服务端自动离线阈值由 `agent.inactive.minutes` 配置（当前 30 分钟）。

---

## 5. 数据结构设计

## 5.1 内存结构

1. `onlineAgents: LinkedHashSet<String>`
2. `customerAgentMap: Map<String, String>`
3. `pendingCustomers: LinkedHashSet<String>`
4. `recordsByCustomer: Map<String, List<ChatMessageRecord>>`
5. `failedBuffer: Map<String, List<AgentPushMessage>>`
6. `loginRecordMap: Map<String, String>`（AuthController 中）

### 选择说明

- `LinkedHashSet`：保持插入顺序，便于队列式分配。
- `ConcurrentHashMap` + `synchronized`：实现并发下的可见性与原子更新。
- 会话关闭时，仅释放本地分配 map，不删除历史，保证客户重新发起后仍可查看全量历史。

---

## 5.2 DTO / Domain

- 登录：`LoginRequest` / `LoginResponse` / `LogoutRequest`
- 聊天：`ChatCustomer` / `ChatMessageRecord` / `ManualReplyRequest` / `ManualMediaReplyRequest` / `AgentCustomerView`
- 推送：`AgentPushMessage` / `WsReconnectRequest`
- CRM同步：`AssignmentRecord` / `CrmChatRecord` / `CrmAgentAccount`

---

## 6. 外部接口对接设计

## 6.1 CRM OpenAPI

统一由 `CrmOpenApiServiceImpl` 封装。

1. `getFilterRows`
   - 登录校验
   - 在线坐席查询
   - 分配记录查询
   - 聊天记录查询
   - 当前服务中分配查询

2. `addRow`
   - 登录记录新增
   - 分配记录新增
   - 聊天记录新增

3. `editRow`
   - 登出/自动离线时更新登录状态为离线
   - 24小时规则更新“是否可回复”
   - 30天规则更新服务状态为“已关闭”

异常策略：
- 所有 CRM 调用均捕获异常并日志记录，返回空或 false，避免主线程崩溃。
- 关键 CRM 调用若返回 false，也会记录 warning，便于定位“无异常但未成功写入”的场景。

---

## 6.2 Dify AI

- 由 `AiService` -> `DifyAiServiceImpl` 调用 `/v1/chat-messages`
- 当前 webhook 使用阻塞 `chat()` 获取完整回复 JSON，再抽取 answer。
- 在“客户未分配坐席”或“客户已分配但坐席离线（保持关系）”场景触发 AI 自动回复。

---

## 6.3 WhatsApp Graph API

- `POST /{phone-number-id}/media`：上传本地文件，返回 `mediaId`。
- `POST /{phone-number-id}/messages`：发送文本/多媒体消息。
- 多媒体消息体兼容两种模式：
  - `id`（推荐，先上传再发送）
  - `link`（兼容旧链路）
- 由 `SendMessageServiceImpl` 基于 `WebClient` 统一封装并异步发送。

---

## 7. 规则与算法细节

## 7.1 轮询分配算法

- 输入：在线坐席顺序集合
- 使用 `roundRobinIndex` 对队列长度取模
- 输出：本次分配坐席

## 7.2 未分配补偿算法

- 若 `assignIfAbsent` 时无在线坐席：客户进入 `pendingCustomers`
- 坐席登录后，循环 `assignOnePendingCustomerToAgent` 直到队列空

## 7.3 24小时规则

- 人工回复规则：
  - 查询 `lastCustomerMessageTime`
  - `Duration.between(last, now).toHours() > 24` 则拒绝人工回复
- 会话关闭规则：
  - 若客户已有分配关系，且本次新消息到达时发现距离上次客户消息已超过24小时
  - 则先关闭旧分配状态，再将本次消息按新会话重新分配处理

## 7.4 推送失败补偿

- `safeSend` 捕获异常，将消息加入 `failedBuffer[agentRowId]`
- `resendFailed` 读取并重发，重发失败继续回写缓冲

---

## 8. 同步机制设计（CRM -> 本地）

触发时机：
1. 服务启动（`@PostConstruct`）
2. 坐席登录前（`/api/auth/login`）

同步内容：
1. 在线坐席集合
2. 客户-坐席分配映射（当前服务中）
3. 聊天历史

同步过程：
1. 拉在线坐席 -> `replaceState.onlineAgents`
2. 拉分配记录 -> `replaceState.assignments`
3. 拉聊天记录 -> `chatHistoryService.replaceAll`

一致性说明：
- 当前不做增量位点，仅做全量覆盖同步。
- 对于已关闭会话，不恢复到本地分配 map，但其聊天历史仍会被恢复。

---

## 9. 服务端 API 详细说明（入参 / 出参 / 使用方式）

> 约定：
> 1. 业务接口统一返回 JSON。
> 2. 登录成功后，前端需保存 `accountRowId`（即坐席 rowId）并在后续请求中按接口参数名传递（多数接口字段名为 `agentRowId`）。
> 3. 下面示例为当前实现下的推荐调用方式，字段以代码 DTO 为准。

### 9.1 认证接口

#### 9.1.1 登录

- **URL**：`POST /api/auth/login`
- **用途**：坐席登录、同步 CRM 缓存、领取待分配会话、触发历史推送。
- **请求头**：`Content-Type: application/json`
- **请求体（LoginRequest）**：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 坐席登录名（CRM 坐席账号） |
| `password` | string | 是 | 坐席登录密码 |

- **成功响应（LoginResponse）**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否登录成功 |
| `message` | string | 登录结果描述 |
| `accountRowId` | string | 坐席在 CRM 账号表中的 rowId（后续核心标识） |
| `relatedUserIds` | string | 关联用户（人员 ID，逗号分隔） |

- **失败场景**：
  - CRM 鉴权失败：`success=false`，`message` 提示账号或密码错误。
  - CRM 不可用：`success=false`，`message` 提示系统繁忙（具体文案按实现）。

- **调用示例**：

```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "agent001",
  "password": "******"
}
```

---

#### 9.1.2 登出

- **URL**：`POST /api/auth/logout`
- **用途**：坐席下线，更新本地在线状态并尝试回写 CRM 登录状态为“离线”。
- **请求头**：`Content-Type: application/json`
- **请求体（LogoutRequest）**：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `agentRowId` | string | 是 | 登录返回的 `accountRowId` |

- **响应**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `message` | string | 结果描述 |

---

### 9.2 会话查询接口

#### 9.2.1 查询坐席客户列表

- **URL**：`GET /api/chat/customers`
- **用途**：获取坐席已分配客户列表（用于会话侧边栏）。
- **查询参数**：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `agentRowId` | string | 是 | 坐席 rowId |

- **响应**：`List<ChatCustomer>`

`ChatCustomer` 关键字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `customerId` | string | 客户标识（当前实现使用手机号） |
| `lastMessage` | string | 最近一条消息摘要 |
| `lastMessageAt` | string(datetime) | 最近消息时间 |

---

#### 9.2.2 查询“服务中”客户视图（含可回复状态）

- **URL**：`GET /api/chat/customers/serving`
- **用途**：获取服务中客户及 24h 可回复状态。
- **查询参数**：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `agentRowId` | string | 是 | 坐席 rowId |

- **响应**：`List<AgentCustomerView>`

`AgentCustomerView` 关键字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `customerId` | string | 客户标识 |
| `serviceStatus` | string | 服务状态（如：服务中） |
| `canReply` | boolean | 是否允许人工回复（24h 规则） |
| `lastMessageAt` | string(datetime) | 最近消息时间 |

---

#### 9.2.3 查询某客户消息历史

- **URL**：`GET /api/chat/messages`
- **用途**：打开会话窗口时拉取消息列表。
- **查询参数**：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `customerId` | string | 是 | 客户标识（与消息记录中的 customerId 对齐） |

- **响应**：`List<ChatMessageRecord>`

`ChatMessageRecord` 关键字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `customerId` | string | 客户标识 |
| `sender` | string | `customer` / `agent` / `ai` |
| `message` | string | 文本内容或多媒体摘要 |
| `timestamp` | string(datetime) | 消息时间 |

---

### 9.3 人工回复接口

#### 9.3.1 文本回复

- **URL**：`POST /api/chat/reply`
- **用途**：坐席发送文本消息给客户。
- **请求头**：`Content-Type: application/json`
- **请求体（ManualReplyRequest）**：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `from` | string | 是 | 企业发送方号码 ID（Meta phone-number-id） |
| `customerId` | string | 是 | 客户标识（接收方号码） |
| `message` | string | 是 | 回复文本 |

- **响应**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否发送成功 |
| `message` | string | 结果描述（含 24h 校验失败提示） |

---

#### 9.3.2 媒体上传

- **URL**：`POST /api/chat/media/upload`
- **用途**：先上传文件到 Meta，换取 `mediaId`，再用于媒体发送。
- **请求头**：`Content-Type: multipart/form-data`
- **表单参数**：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `from` | string | 是 | 企业发送方号码 ID（Meta phone-number-id） |
| `mediaType` | string | 是 | 媒体类型，如 image/video/audio/document |
| `file` | file | 是 | 待发送文件 |

- **成功响应**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否上传成功 |
| `mediaId` | string | Meta 返回的媒体 ID |
| `filename` | string | 上传文件名 |
| `mediaType` | string | 上传媒体类型 |

---

#### 9.3.3 媒体回复

- **URL**：`POST /api/chat/reply/media`
- **用途**：发送图片/视频/文档等媒体消息，兼容 `mediaId` 与 `mediaUrl` 两种模式。
- **请求头**：`Content-Type: application/json`
- **请求体（ManualMediaReplyRequest）**：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `from` | string | 是 | 企业发送方号码 ID（Meta phone-number-id） |
| `customerId` | string | 是 | 客户标识（接收方号码） |
| `mediaType` | string | 是 | `image`/`video`/`audio`/`document` 等 |
| `mediaId` | string | 条件必填 | 上传后返回的媒体 ID（优先） |
| `mediaUrl` | string | 条件必填 | 外链地址（与 mediaId 至少填一个） |
| `caption` | string | 否 | 媒体说明文字 |
| `filename` | string | 否 | 文档文件名（document 场景建议带上） |

- **响应**（当前实现）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否发送成功（成功时通常仅返回此字段） |
| `message` | string | 失败时返回错误原因（参数校验/24h 规则） |

---

### 9.4 WebSocket 补偿接口

#### 9.4.1 重连后失败消息重推

- **URL**：`POST /api/agent/ws/reconnected`
- **用途**：前端 WebSocket 重连成功后，通知服务端重发失败缓冲消息。
- **请求头**：`Content-Type: application/json`
- **请求体（WsReconnectRequest）**：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `agentRowId` | string | 是 | 坐席 rowId |

- **响应**：`200 OK`（无 body）
> 说明：该接口当前返回空响应体，前端可通过 HTTP 200 判定调用成功。

#### 9.4.2 活跃心跳

- **URL**：`POST /api/agent/activity/ping?agentRowId=...`
- **用途**：前端上报坐席活跃，降低自动离线误判。
- **响应**：`200 OK`（无 body）

---

### 9.5 WhatsApp Webhook 接口

#### 9.5.1 Webhook 验证

- **URL**：`GET /webhook`
- **用途**：Meta 平台校验 webhook 地址可用性。
- **查询参数**（Meta 标准）：
  - `hub.mode`
  - `hub.verify_token`
  - `hub.challenge`
- **成功响应**：原样返回 `hub.challenge`。

#### 9.5.2 消息回调入口

- **URL**：`POST /webhook`
- **用途**：接收客户消息/状态回调，进入系统主流程（落库、分配、推送、AI）。
- **请求体**：Meta 回调 JSON（较大，建议原样透传）。
- **响应**：`200 OK`（成功接收后异步处理）。

---

### 9.6 前端推荐调用时序

1. 登录页调用 `POST /api/auth/login`。
2. 登录成功后建立 WebSocket 连接并订阅：
   - `/topic/agent/{agentRowId}`
3. 调 `GET /api/chat/customers/serving` 渲染会话列表。
4. 打开会话时调 `GET /api/chat/messages` 拉历史。
5. 发送文本：`POST /api/chat/reply`。
6. 发送媒体：
   - 先 `POST /api/chat/media/upload` 获取 `mediaId`
   - 再 `POST /api/chat/reply/media` 发送媒体消息
7. WebSocket 断线重连成功后调 `POST /api/agent/ws/reconnected`。
8. 前端定时上报 `POST /api/agent/activity/ping`（建议 20~30 秒一次）。
9. 退出时调 `POST /api/auth/logout`。

---

## 10. 配置项

- `crm.base-url`
- `crm.appKey`
- `crm.sign`
- `crm.ownerId`
- `whatsapp.*`
- `ai.dify.*`

---

## 11. 当前阶段实施结果

1. 一阶段核心能力已开发完成并上线。
2. 已形成“客户接入 -> AI响应 -> 人工接管 -> CRM记录 -> WebSocket推送”的完整主链路。
3. 已具备多媒体、会话超时关闭、重新分配等补充能力。
4. 当前整体更适合单实例运行，后续可在稳定基础上演进为高可用架构。

---

## 12. 可靠性与风险

1. 内存态在单实例可用，多实例下需要外部共享存储。
2. loginRecordMap 在重启后丢失，可能导致历史登录记录无法离线更新。
3. CRM 返回字段结构可能变化，解析逻辑需增强健壮性。
4. WebSocket 当前无鉴权，生产环境需补认证与权限。
5. 会话关闭依赖客户历史时间判断，若 CRM/本地历史异常缺失，可能影响关闭时机准确性。

---

## 13. 演进建议

1. 引入 DB / Redis 持久化分配与历史。
2. 登录记录映射持久化，保证登出状态可追踪更新。
3. 增加任务重试、死信与监控告警。
4. WebSocket 鉴权 + 订阅授权。
5. 为 CRM 读取引入分页与增量游标，避免全量同步压力。


---

## 14. CRM工作表与字段映射（对齐甲方配置）

### 14.1 通用接口

- `POST /api/v2/open/worksheet/addRow`
- `POST /api/v2/open/worksheet/getFilterRows`
- `POST /api/v2/open/worksheet/editRow`
- `POST /api/v2/open/worksheet/deleteRow`

### 14.2 当前实现已对接工作表

1. `imzhgl`（坐席账号管理）
2. `zxzt`（坐席状态管理）
3. `ltjl`（坐席分配记录）
4. `ltjl1`（聊天记录）

> 说明：`aijdc`（AI接待池）与 `zxdjgl`（坐席等级管理）已纳入目标数据模型，但当前代码阶段尚未完整接入其写入/负载算法。

### 14.3 控件类型约束（重点）

- 文本：`dataType=2`
- 单选：`dataType=11`
- 时间：`dataType=16`
- 关联记录：`dataType=29`（值为 rowId，多值逗号分隔）
- 富文本：`dataType=41`

### 14.4 数据一致性与并发要求

1. 分配成功后至少保证：
   - `ltjl` 分配记录写入（服务中）
   - 客户消息入 `ltjl1`
2. 会话超时关闭后：
   - `ltjl` 服务状态更新为“已关闭/已结束（按甲方枚举落地）”
   - 本地分配关系释放
3. 分配逻辑必须保证：
   - 不超过坐席负载
   - 不重复分配
   - 并发下状态可重入与可追溯

---

## 15. AI优先接待 + 人工分层分配（新增目标方案）

> 本章节为在现有一期实现基础上的增强设计，目标是形成“AI优先承接 + 分层人工接管 + 超时回收AI”的完整闭环。

### 15.1 系统目标

构建一套 **AI 优先接待 + 人工坐席分层分配 + 超时回收机制** 的会话分配系统，实现：

1. 新客户优先由 AI 接待。
2. AI 与人工坐席协同处理会话。
3. 按“等级 + 权重 + 负载”动态分配人工坐席。
4. 坐席未及时回复自动回收至 AI。
5. 非工作时间由 AI 兜底服务，保证不漏接。

---

### 15.2 基础数据配置

#### 15.2.1 坐席等级管理表（`zxdjgl`）

| 字段ID | 字段名称 | 类型 | 控件类型编号 | 说明 |
| --- | --- | --- | --- | --- |
| `69ca5008433ec9f4b5e7fc14` | 坐席等级 | 文本框 | 2 | String |
| `69ca5263433ec9f4b5e7fc9d` | 坐席权重 | 数值 | 6 | Double（示例：50/30/20） |
| `69ca5313433ec9f4b5e7fcab` | 负载 | 数值 | 6 | Double（同一时间最大接待数） |
| `rowid` | 记录ID | 文本框 | 2 | String |

#### 15.2.2 测试参数建议

1. 高级：权重 50%，负载 10
2. 中级：权重 30%，负载 8
3. 初级：权重 20%，负载 5

---

### 15.3 系统初始化逻辑

服务启动时执行：

1. 加载 `zxdjgl`（等级 / 权重 / 负载）配置并缓存。
2. 初始化等级默认系数（固定规则）：
   - 高级：`1.1`
   - 中级：`1.0`
   - 初级：`0.9`
3. 初始化层级状态（在线人数、当前承载、轮询指针）用于后续分配。

---

### 15.4 核心分配机制（两层分配）

#### 15.4.1 第一层：层级选择

新消息触发人工分配时，先在“高级/中级/初级”三个层级中选择目标层级。

层级分值公式：

`分值 = 等级默认系数 × 坐席权重 × (当前在线人数 × 负载 - 当前层级已分配会话数)`

其中：

- `当前在线人数 × 负载 - 当前层级已分配会话数` 即剩余容量。
- 若剩余容量 `<= 0`，该层级不可参与分配。
- 在可分配层级中按分值从高到低选择第一名。

#### 15.4.2 第二层：层级内坐席选择

在已选层级内执行：

1. 过滤已满负载坐席。
2. 使用平均轮询（Least Recently Assigned）：
   - 最近最久未被分配的坐席优先。
3. 更新坐席当前承载数、层级承载数与轮询游标。

---

### 15.5 AI 接待池机制

#### 15.5.1 AI接待池表（`aijdc`）

| 字段ID | 字段名称 | 类型 | 控件类型编号 | 说明 |
| --- | --- | --- | --- | --- |
| `69cb3e3b433ec9f4b5e80433` | 客户电话 | 文本框 | 2 | String |
| `69cb3ff3433ec9f4b5e80476` | 分配时间 | 日期时间 | 16 | String（yyyy-MM-dd HH:mm:ss） |
| `69cb3ff3433ec9f4b5e80477` | 分配原因 | 单选 | 11 | 首次会话/未及时回复/非工作日 |
| `69cb3ff3433ec9f4b5e80478` | 服务状态 | 单选 | 11 | 服务中/已关闭 |
| `69cb3ff3433ec9f4b5e80479` | 转出时间 | 日期时间 | 16 | String（yyyy-MM-dd HH:mm:ss） |
| `rowid` | 记录ID | 文本框 | 2 | String |

#### 15.5.2 AI接待触发规则

新消息进入后默认先入 AI 接待池，写入：

- 客户电话
- 分配时间
- 服务状态：`服务中`
- 分配原因：`首次会话`

#### 15.5.3 AI处理策略

1. **首次会话**
   - AI 仅处理一轮回复；回复后尝试转人工。
   - 若分配成功：AI接待状态更新为 `已关闭`；写转出时间。
   - 若分配失败：保持 AI 持续服务。

2. **未及时回复回收**
   - 坐席超时未回复则回收至 AI。
   - 分配原因记录为 `未及时回复`。
   - 回收后该会话默认由 AI 持续服务，不再自动转人工。

3. **非工作时间兜底**
   - 若无可用坐席，直接 AI 承接。
   - 分配原因记录为 `非工作日`。
   - AI 持续服务。

---

### 15.6 超时机制

#### 15.6.1 5分钟未回复提醒

条件：客户消息进入人工会话后，坐席 5 分钟未回复。
动作：向 IM 前端推送提醒，提示当前责任坐席尽快处理。

#### 15.6.2 10分钟未回复回收

条件：客户消息进入人工会话后，坐席 10 分钟未回复。
动作：

1. 释放该客户与坐席的绑定关系。
2. 会话回收至 AI 接待池。
3. 在 `aijdc` 写入/更新：
   - 分配原因：`未及时回复`
   - 服务状态：`服务中`

---

### 15.7 非工作时间策略

规则：

1. 若处于非工作时间：
   - 有可用坐席：允许正常分配；
   - 无可用坐席：AI 兜底。
2. AI 兜底时分配原因统一记录为 `非工作日`。

---

### 15.8 异常处理机制

1. **坐席下线（主动登出 / 掉线）**
   - 释放该坐席所有会话。
   - 优先回收到 AI 接待池，再按策略尝试重新分配。

2. **无可分配坐席**
   - 会话全部交由 AI 承接。

3. **坐席满负载**
   - 满负载坐席不参与本轮分配。
   - 会话进入 AI 兜底并记录原因。

---

### 15.9 会话流转状态

#### 15.9.1 AI接待池状态

`服务中 -> 已关闭`

#### 15.9.2 会话路径

1. 首次会话：`AI服务 -> 分配中 -> 人工服务`
2. 异常场景：`人工服务 -> 超时未回复 -> AI接管`

---

### 15.10 端到端流程总结

新消息进入：

1. 进入 AI 接待池（首次会话）。
2. AI 回复 1 轮。
3. 进入人工分配引擎。
4. 计算层级分值并选择层级。
5. 在层级内轮询选择坐席。
6. 分配成功则人工接管并关闭 AI 状态；失败则 AI 持续服务。

---

### 15.11 关键设计原则

1. AI 作为流量缓冲层（削峰）。
2. 人工作为核心转化层。
3. 分配遵循“权重 + 负载 + 动态容量”。
4. 异常场景必须可回退到 AI。
5. 保证系统不出现“无人接待”。
7. `AgentInactiveScheduler`
   - 30 分钟无活跃自动离线
   - 自动离线时，若会话最后一条为客户消息，触发 AI 兜底回复
